{
  "name": "geometry-helpers",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "types": "dist/index.d.ts",
  "scripts": {
    "dev": "vite",
    "build": "tsc",
    "build:vite": "tsc && vite build",
    "build:watch": "tsc && vite build --watch",
    "preview": "vite preview",
    "reloadTypes": "rm -rf node_modules/ && yarn"
  },
  "devDependencies": {
    "type-library": "file:../type-library/",
    "typescript": "^5.0.2",
    "vite": "^4.3.2"
  }
}
